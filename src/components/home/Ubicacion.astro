---
import Container from "../shared/Container.astro";
import { WHATSAPP, DIRECCION, FACEBOOK, EMAIL } from "../../consts";

const contactInfo = {
    address: DIRECCION,
    whatsapp: WHATSAPP,
    email: EMAIL,
    facebook: FACEBOOK,
};
---

<section
    class="section ubicacion"
    itemscope
    itemtype="http://schema.org/Service"
>
    <Container>
        
        <div class="contact-section">
            <div class="contact-info">
                <p>
                    Estamos aquí para ti. Desde consultas hasta compras, nuestra
                    prioridad es que te comuniques fácilmente con nosotros.
                    RaizViva, siempre a tu alcance para ofrecerte calidad y
                    confianza.
                </p>
                <span class="flex flex-row justify-start"
                    ><img
                        class="w-6 h-6 mr-2"
                        src="/images/icons/location.svg"
                        alt=""
                    /><p>
                        {contactInfo.address}
                    </p></span
                >
                <span class="flex flex-row justify-start">
                    <img
                        class="w-6 h-6 mr-2"
                        src="/images/icons/whatsapp.svg"
                        alt=""
                    />
                    <a
                        class="mb-4"
                        href={`https://wa.me/${contactInfo.whatsapp}`}
                        target="_blank">{contactInfo.whatsapp}</a
                    >
                </span>
                <span class="flex flex-row justify-start">
                    <img
                        class="w-6 h-6 mr-2"
                        src="/images/icons/email.svg"
                        alt=""
                    />

                    <a class="mb-4" href={`mailto:${contactInfo.email}`}
                        >{contactInfo.email}</a
                    >
                </span>
                <span class="flex flex-row justify-start">
                    <img
                        class="w-6 h-6 mr-2"
                        src="/images/icons/facebook.svg"
                        alt=""
                    />

                    <a  class="mb-4" href={contactInfo.facebook} target="_blank">Visítanos</a>
                </span>
            </div>

            <div class="map-container">
                <div class="map">
                    <iframe
                        src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d4701.637473899523!2d-76.44849238861275!3d-12.220207387980423!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x910f63004a6288a7%3A0x76898420e8b89ce3!2sRaizViva!5e1!3m2!1ses-419!2spe!4v1750975071601!5m2!1ses-419!2spe"
                        width="100%"
                        height="100%"
                        style="border:0;"
                        allowfullscreen=""
                        title="Ubicación de RaizViva"
                        loading="lazy"
                        referrerpolicy="no-referrer-when-downgrade"></iframe>
                </div>
            </div>
        </div>
    </Container>
</section>

<style>
    .contact-section {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .contact-info {
        flex: 1 1 100%;
        padding: 1rem;

        border-radius: 8px;
    }

    .contact-info img {
        filter: var(--rv-accent-filter);
    }

    .map-container {
        flex: 2 1 100%;
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .map {
        flex: 1;
        border-radius: 8px;
        overflow: hidden;
    }

    @media (min-width: 768px) {
        .contact-section {
            flex-wrap: nowrap;
        }

        .contact-info {
            flex: 1;
        }

        .map-container {
            flex: 2;
            flex-direction: row;
        }
    }
</style>
