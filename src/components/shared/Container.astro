---
export interface Props {
    className?: string;
}

const { className } = Astro.props;
---

<div
    class={`mx-auto max-w-7xl relative w-full px-2 sm:px-3 md:px-4 lg:px-5 ${className}`}
>
    <slot />
</div>
